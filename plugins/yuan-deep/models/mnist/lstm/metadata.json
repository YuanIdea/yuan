{
  "modelConfig": {
    "name": "lstm",
    "inputShape": [28, 28],
    "layers": [
      {"type": "lstm", "units": 16, "numLayers":2},
      {"type": "flatten"},
      {"type": "dense", "units": 64, "activation": "relu"},
      {"type": "dropout", "rate": 0.5},
      {"type": "dense", "units": 10, "activation": "linear"}
    ]
  },
  "training": {
    "batchSize": 64,
    "loss":"mse",
    "epochs": 5
  }
}